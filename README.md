# Github Codespaces

`lsblk` shows a 512 gigabyte drive that can be used for extra storage
In `.devcontainer/devcontainer.json` you can use "initializeCommand" to access the Docker account of `codespacesdev`
